---
title: 环境配置-机器学习(待完成)
date: 2019-05-16 21:37:10
tags: 配置
grammar_cjkRuby: true
---

### *0x00 引言*
&emsp;&emsp; 机器学习常用库TensorFlow和PyTorch，为避免忘记开发环境的配置，这里记录一下。

### *0x01 Tensorflow环境配置*
&emsp;&emsp; TensorFlow由谷歌人工智能团队谷歌大脑开发和维护，是一种开源的神经网络算法库。由于网上的配置资料又多而且奇葩，这里以[DeepFakes的安装教程](https://github.com/deepfakes/faceswap/blob/master/INSTALL.md)为例，解释如何配置。
```
1 Win10环境下安装
  1.1 Anaconda安装
  1.2 Anaconda环境生成
  1.3 Git和VSCode安装
  1.4 DeepFakes
  1.5 TensorFlow
  1.6 VSCode配置
```

***1、Win10环境下安装***  
***1.1、Anaconda安装***  
&emsp;&emsp; Anaconda是一个开源的包、环境管理器，它的优势在于能够配置不同版本python环境，同时集成一键安装各种依赖包和IDE等常用工具，其中包括我们常用的Git、VSCode等软件，所以以后装机的时候完全可以先装一个Anaconda，再用它去管理我们的常用开发工具。  
&emsp;&emsp;其安装方式较简单，查阅[百度经验](https://jingyan.baidu.com/article/0202781124ef4a1bcc9ce5cd.html)，注意事项如下：  
&emsp;&emsp;1)右键管理员权限方式打开安装包;  
&emsp;&emsp;2)个人计算机还是勾选Just Me;  
&emsp;&emsp;3)取消勾选Add Anaconda to my PATH environment variable。由于该项勾选后，后续需要卸载Anaconda时，将需要手动删除很多路径，因此推荐在安装完毕后自己手动添加环境变量(其实就是系统的链接库查询路径)。  
&emsp;&emsp;安装完成后，根据需要[手动添加环境变量](https://www.cnblogs.com/jpfss/p/8608204.html)，目的就是为了在CMD或PowerShell环境下识别Conda指令，但实际上也没有必要，原因如下：  
&emsp;&emsp;1)我们通常情况下会使用Anaconda自带的终端环境Anaconda Powershell Prompt，这不会产生任何错误;  
&emsp;&emsp;2)就算按上述手动添加环境变量，在CMD或PowerShell环境下还是不能使用"conda activate"或"conda deactivate"命令，还需要在终端运行"conda init"指令，该指令是用来初始化终端和Anaconda环境链接关系的;   
&emsp;&emsp;3)就算运行“conda init”指令了，CMD变得可以使用"conda activate"或"conda deactivate"命令，但是PowerShell仍然不行，还需要借用[其他的技术](http://yuhao.me/zai-powershellzhong-shi-yong-condade-activate.html)，我已经踩坑这里就别试了，这个方法用完之后，在我的CMD里添加了初始化运行环境，不知道怎么删(类似于Ubuntu下的/.bashrc)，导致每次打开终端都会显示一段错误提示。  
&emsp;&emsp;总结，不改配置环境也行，使用Anaconda自带的终端环境Anaconda Powershell Prompt，且在后续我会介绍怎么配置VSCode内运行Prompt。  
&emsp;&emsp;若要添加环境变量，如下：
```py
#Anaconda3为安装目录
\Anaconda3
\Anaconda3\Scripts
\Anaconda3\Library\bin
```
***1.2、Anaconda环境生成***  
&emsp;&emsp;Anaconda最大的优势就在于能配置多个Python环境，而由于TensorFlow不同版本只能运行在指定Python版本下，比如这里我们就需要Python3.6的环境，那么就使用Anaconda生成一个独立的环境(注意，每个环境都有自己独立的开发工具，比如就算在base环境下安装了Git，那么在新环境下仍然重新安装)。具体操作流程查阅[博文](https://www.cnblogs.com/W-Yentl/p/7783933.html)，这里简单码一下代码：  
```py
#首先打开Anaconda Prompt
#添加配置清华源
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
conda config --set show_channel_urls yes
#直接添加一个环境，取名为faceswap，且依赖于Python3.6
conda create -n faceswap python=3.6
#启动该环境
conda activate faceswap
```
&emsp;&emsp;注意，每次打开Prompt或者Anaconda都需要重新激活环境，当然我们也可以使用Anaconda Navigater直接界面模式操作(配置包的时候推荐这么做，很方便快捷)。  
***1.3、Git和VSCode安装***  
&emsp;&emsp;正如之前所说，开发环境最后都在Anaconda内配置，所以如果电脑上未安装Git，这里可以直接从Anaconda Navigater里在该环境下添加Git。但是如果电脑里已经有了Git，那么最好不要重复安装，否则由于注册表或者环境变量的问题，会导致电脑不知道运行哪个好，且软件版本不好控制，对电脑的速度也会有影响。  
&emsp;&emsp;我还是推荐自己独立去安装Git和VSCode，然后再自己做配置。对于本教程来说，Git用来clone DeepFakes，VSCode用来搭建编译环境。  
***1.4、DeepFakes***  
&emsp;&emsp;由于本教程是以DeepFakes软件安装为例，所以这里添加该节，如果不需要玩AI换脸，则跳过此节。安装细节全在[DeepFakes git库](https://github.com/deepfakes/faceswap/blob/master/INSTALL.md)，**其实本文基本上是在该安装指导下完成的，但是它有一个小坑，是在pip安装TensorFlow那步，后面叙述**。现在讲一下其下载过程：
```py
#首先clone DeepFakes，由于我的Git是安装在Anaconda外的，因此自行打开Git Bash，clone至任意位置
git clone --depth 1 https://github.com/deepfakes/faceswap.git
#打开Anaconda Prompt并进入faceswap环境
#由于我直接放在初始位置下，所以直接cd
cd faceswap
#这步重要，不管你打不打算Manual install，都要试一遍Easy install，若失败再手动安装
python setup.py
#成功则结束，不成功继续手动安装。若没有试过简易安装，则requirements.txt安装不了，不知为何
conda install tk
pip install -r requirements.txt
```
***1.5、TensorFlow***  
&emsp;&emsp;最后一步，也是最重要的一步，花费时间较长。注意这里有坑，如果直接再Anaconda里用pip安装tensorflow，则运行py文件时，会导致无法找到TensorFlow链接库(至今不知道如何解决，原则上直接在环境变量下添加其下载位置即可，到那时又不知道怎么加)，因此我们改为直接使用Conda方法将TensorFlow安装至我们在1.2节配置的环境之下，这样Anacodna Prompt终端会自动识别这些新的包。  
&emsp;&emsp;这里要注意我们首先要确定自己的电脑显卡究竟适合GPU还是CPU版本的TensorFlow，**同样的任务前者计算时间需要几小时，后者可能就是一星期**。GPU版本需要先安装CUDA和CUDNN，如果想使用，首先要查看显卡究竟适不适合GPU运算，其对应CUDA的算力直接查阅[NVIDIA官网](https://developer.nvidia.com/cuda-gpus)(现在只有NVIDIA开源了其CUDA编译器，所以才能够借用GPU之力做机器学习，**且必须选用对应算力的CUDA与CUDNN，否则报错**)。  
具体安装方式如下：
```py
#根据需要安装自己想要的版本
#GPU版TensorFlow
conda install tensorflow-gpu
#CPU版TensorFlow
conda install tensorflow
```
***1.6、VSCode配置***  
&emsp;&emsp;VSCode应该是目前用过最节简的IDE了，体积小，运行快，可自动添加各种插件还开源。如果总是使用Anaconda Prompt或者从Anaconda Navigator运行环境则稍显麻烦，这里简述一下如何将Anaconda环境外安装的VSCode链接至Anaconda并搭建自己的环境。  
&emsp;&emsp;1)首先下载依赖包-Python;  
&emsp;&emsp;2)选择File-Preferences-Settings，键入python.python Path，修改路径为/Anaconda3/python.exe，即根目录下的python可执行文件;  
&emsp;&emsp;3)此时在IDE内打开任意.py文件，IDE左下角便可以选择使用哪个版本Python，点击选择我们在1.2节中搭建环境的Python3.6版本;  
&emsp;&emsp;4)键入快捷键"CTRL + ~"，打开终端界面，默认使用PowerShell(前面介绍过，使用该终端并不能activate环境)，但是应该会提示更换Prompt版本终端，更改之后就相当于一款内置于VSCode的Anaconda Prompt了。

### *0x02 PyTorch环境配置*
```
1、PyTorch安装
2、PyCharm安装
```
***1 PyTorch安装***
&emsp;&emsp;PyTorch基于Ubuntu16.04配置环境。  
```sh
#先安装pip3
sudo apt-get install python3-pip
#更新pip3版本
sudo pip3 install --upgrade pip
#去官网 https://pytorch.org 查安装代码(根据个人系统情况)
pip3 install torch==1.4.0+cpu torchvision==0.5.0+cpu -f https://download.pytorch.org/whl/torch_stable.html
#安装numpy
pip3 install --upgrade numpy
```

***2 PyCharm安装***  
&emsp;&emsp;PyCharm安装，用于编写python程序的IDE，具体安装方式查阅Ubuntu16.04配置sh包。

### *0x03 引用文献*